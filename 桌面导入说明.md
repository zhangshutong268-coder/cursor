# 桌面四文件夹 → 仓库路径

云端 Agent **看不到**你电脑桌面。本机把四个文件夹按下面路径放进本仓库后，再说一声「已放进仓库」，即可继续细拆。

| 桌面文件夹 | 归入路径 | 说明 |
|------------|----------|------|
| `童童学习` | [`孩子资料/童童学习/`](孩子资料/童童学习/) | 孩子学习资料 |
| `语言学习` | [`自己学习/语言学习/`](自己学习/语言学习/) | 你自己的语言学习 |
| `赚钱` | [`投资盯盘/赚钱/`](投资盯盘/赚钱/) | 赚钱 / 盯盘相关素材 |
| `假死三年` | [`写小说/假死三年/`](写小说/假死三年/) | 小说项目原稿与资料 |

## 本机一键导入（推荐）

在**已经 `git pull` 过本仓库**的电脑上，把四个文件夹仍放在「桌面」，然后：

**Mac / Linux（终端）：**

```bash
cd /path/to/cursor   # 本仓库根目录
chmod +x scripts/import-desktop-folders.sh
./scripts/import-desktop-folders.sh
```

**Windows（PowerShell）：**

```powershell
cd C:\path\to\cursor
.\scripts\import-desktop-folders.ps1
```

默认从当前用户桌面复制（不删除原文件夹）。可用环境变量改源目录，例如：

```bash
DESKTOP_DIR="$HOME/Desktop" ./scripts/import-desktop-folders.sh
```

## 手动拖拽

把桌面上的四个文件夹直接拖进上表右侧目录（或拖到仓库根目录后告诉我，我再帮你挪到正确路径）。
